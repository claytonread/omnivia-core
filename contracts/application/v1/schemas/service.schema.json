{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.omnivia.dev/application/v1/service.schema.json",
  "title": "OmniVia Core Application Contract v1 - service runtime probes",
  "description": "Health, readiness, and discovery probe request/result shapes. These are transport-level infrastructure endpoints, not application operations: a probe carries no workspace scope, no caller authority, and no operation payload, so it can be answered before authentication or workspace negotiation completes. This document declares no root type; it is a definition container only.",
  "$defs": {
    "ProbeKind": {
      "title": "ProbeKind",
      "description": "Open, dot-namespaced code naming which runtime probe is being requested or answered. The frozen, currently known probe kinds are exactly `service.health`, `service.readiness`, and `service.discover`. Open by design so a compatible minor release can add probe kinds without breaking existing callers.",
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z][a-z0-9_]*(?:\\.[a-z][a-z0-9_]*)*$(?![\\s\\S])"
    },
    "ProbeStatus": {
      "title": "ProbeStatus",
      "description": "Open, dot-namespaced code naming the outcome of a probe or one of its components, such as `pass` or `warn` or `fail`. Open by design; an unrecognized status must be preserved and surfaced, not coerced to a known one.",
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z][a-z0-9_]*(?:\\.[a-z][a-z0-9_]*)*$(?![\\s\\S])"
    },
    "ServiceProbeRequest": {
      "title": "ServiceProbeRequest",
      "description": "A request to answer one runtime probe. Deliberately distinct from `RequestEnvelope`: it carries no `operation`, no `input`, and no workspace or authority scoping, because a probe must be answerable before those concepts apply.",
      "type": "object",
      "properties": {
        "probe": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ProbeKind",
          "description": "Which probe is being requested."
        },
        "request_id": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/RequestId",
          "description": "Identifies this probe attempt, for correlating logs across a health check."
        },
        "deadline_ms": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/DurationMs",
          "description": "Relative time budget for answering the probe."
        }
      },
      "required": ["probe"],
      "unevaluatedProperties": false
    },
    "ServiceComponentStatus": {
      "title": "ServiceComponentStatus",
      "description": "The health of one subsystem a readiness or health probe inspected.",
      "type": "object",
      "properties": {
        "id": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Identifier",
          "description": "Stable identifier of the subsystem, such as `storage` or `search_index`."
        },
        "status": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ProbeStatus",
          "description": "Outcome for this subsystem."
        },
        "observed_at": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Timestamp",
          "description": "When this subsystem was last checked."
        },
        "message": {
          "type": "string",
          "description": "Human-readable explanation. Not a stable interface.",
          "maxLength": 2048
        },
        "details": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/JsonObject",
          "description": "Optional structured detail."
        }
      },
      "required": ["id", "status", "observed_at"],
      "unevaluatedProperties": false
    },
    "ServiceProcessEvidence": {
      "title": "ServiceProcessEvidence",
      "description": "Evidence naming the operating-system process that published a descriptor, so a stale descriptor left behind by a crashed instance can be recognized rather than trusted. All three facts are required together: a pid alone is ambiguous once the host reuses it, and it is the start time and boot identifier that make the triple a stable identity. This names a process; it does not grant access to one, and it never carries a filesystem or database location.",
      "type": "object",
      "properties": {
        "pid": {
          "type": "integer",
          "description": "Operating-system process identifier of the running service instance.",
          "minimum": 1
        },
        "start_time": {
          "type": "string",
          "description": "Opaque, platform-defined process start time, compared only for equality against a later reading of the same pid. Never parsed: its spelling is whatever the host platform reports.",
          "minLength": 1,
          "maxLength": 128
        },
        "boot_id": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Identifier",
          "description": "Identifies the host boot during which the pid and start time were read, so the pair is never matched against a reading taken after a restart."
        }
      },
      "required": ["pid", "start_time", "boot_id"],
      "unevaluatedProperties": false
    },
    "ServiceEndpointUri": {
      "type": "string",
      "description": "Canonical dialable Core transport endpoint. Lowercase HTTP and HTTPS require a valid host and an optional port in 1-65535 written without leading zeros, and carry no query and no fragment. Local IPC uses an absolute `.sock` Unix-domain socket URI or a safe Windows named-pipe URI. URI userinfo, direct-storage schemes, credential-bearing queries, fragments and unapproved transports are forbidden. This pattern is the single authority for the policy: every generated binding compiles it directly, so no runtime adds acceptance rules of its own.",
      "format": "uri",
      "pattern": "^(?:https?://(?:(?:25[0-5]|2[0-4][0-9]|1[0-9]{2}|[1-9]?[0-9])(?:\\.(?:25[0-5]|2[0-4][0-9]|1[0-9]{2}|[1-9]?[0-9])){3}|[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?(?:\\.[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?)*|\\[(?:(?:[0-9A-Fa-f]{1,4}:){7}[0-9A-Fa-f]{1,4}|(?:[0-9A-Fa-f]{1,4}:){1,7}:|(?:[0-9A-Fa-f]{1,4}:){1,6}:[0-9A-Fa-f]{1,4}|(?:[0-9A-Fa-f]{1,4}:){1,5}(?::[0-9A-Fa-f]{1,4}){1,2}|(?:[0-9A-Fa-f]{1,4}:){1,4}(?::[0-9A-Fa-f]{1,4}){1,3}|(?:[0-9A-Fa-f]{1,4}:){1,3}(?::[0-9A-Fa-f]{1,4}){1,4}|(?:[0-9A-Fa-f]{1,4}:){1,2}(?::[0-9A-Fa-f]{1,4}){1,5}|[0-9A-Fa-f]{1,4}:(?:(?::[0-9A-Fa-f]{1,4}){1,6})|:(?:(?::[0-9A-Fa-f]{1,4}){1,7}|:))\\])(?::(?:[1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5]))?(?:/(?:[A-Za-z0-9\\-._~!$&'()*+,;=:@]|%[0-9A-F]{2})*)*|unix:///(?!\\.{1,2}(?:/|$))(?!.*?/\\.{1,2}(?:/|$))(?!.*//)(?!.*%2[EF])(?:[A-Za-z0-9\\-._~!$&'()*+,;=:@]|%[0-9A-F]{2})+(?:/(?:[A-Za-z0-9\\-._~!$&'()*+,;=:@]|%[0-9A-F]{2})+)*\\.sock|pipe://[A-Za-z0-9](?:[A-Za-z0-9._-]{0,198}[A-Za-z0-9])?)$(?![\\s\\S])",
      "minLength": 1,
      "maxLength": 2048
    },
    "ServiceEndpointDescriptor": {
      "title": "ServiceEndpointDescriptor",
      "description": "The published coordination facts a client needs to find one running service instance and decide whether it can talk to it, before any request is sent. Coordination data only: a descriptor carries no bearer credential or token, no granted or effective capability authority, no lease ownership, and no database or workspace storage location. A local IPC transport address may contain its bounded socket path. Holding one lets a caller address an endpoint and negotiate versions; it authorizes nothing, and every authority question is still settled by an authenticated request.",
      "type": "object",
      "properties": {
        "descriptor_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ContractVersion",
          "description": "Contract version of the descriptor shape itself, so a reader can tell a descriptor it fully understands from a newer one it must read conservatively."
        },
        "workspace_id": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/WorkspaceId",
          "description": "Workspace this service instance is serving."
        },
        "service_instance_id": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Identifier",
          "description": "Identifies this running service instance. A restart is a new instance and gets a new identifier."
        },
        "installation_id": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Identifier",
          "description": "Identifies the installation the instance belongs to, which survives restarts and instance identity changes."
        },
        "endpoint_uri": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ServiceEndpointUri",
          "description": "Approved dialable Core transport URI. It never carries userinfo and never names a database file or workspace directory. A `unix:///` value names only a bounded `.sock` transport address; Runtime publication additionally requires it to match the transport-owned endpoint source."
        },
        "protocol_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ContractVersion",
          "description": "Wire protocol version spoken at this endpoint."
        },
        "server_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ReleaseVersion",
          "description": "Concrete server build version publishing this descriptor."
        },
        "supported_api_versions": {
          "$ref": "https://contracts.omnivia.dev/application/v1/compatibility.schema.json#/$defs/VersionWindow",
          "description": "Inclusive API contract version window this instance supports."
        },
        "supported_workspace_versions": {
          "$ref": "https://contracts.omnivia.dev/application/v1/compatibility.schema.json#/$defs/VersionWindow",
          "description": "Inclusive workspace format version window this instance supports."
        },
        "workspace_format_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ContractVersion",
          "description": "Concrete workspace format version the served workspace is stored at."
        },
        "ready": {
          "type": "boolean",
          "description": "Whether the instance is ready to serve requests now. A descriptor may be published before readiness, so this stays a fact of its own rather than being implied by the descriptor existing."
        },
        "lifecycle_state": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/OpenCode",
          "description": "Open, dot-namespaced code naming where the instance is in its lifecycle, such as `starting` or `serving` or `draining`. Open by design; an unrecognized state must be preserved and surfaced, not coerced to a known one."
        },
        "fencing_generation": {
          "type": "integer",
          "description": "Monotonic generation of this instance's claim on the workspace. A reader holding a lower generation is looking at a superseded instance. It records which claim is newer; it neither states nor transfers ownership of one.",
          "minimum": 1
        },
        "published_at": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Timestamp",
          "description": "When this descriptor was published."
        },
        "process": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ServiceProcessEvidence",
          "description": "Local process evidence, when the publisher can observe it. Optional as a whole and complete when present, so a reader never has to reason about a half-identified process."
        }
      },
      "required": [
        "descriptor_version",
        "workspace_id",
        "service_instance_id",
        "installation_id",
        "endpoint_uri",
        "protocol_version",
        "server_version",
        "supported_api_versions",
        "supported_workspace_versions",
        "workspace_format_version",
        "ready",
        "lifecycle_state",
        "fencing_generation",
        "published_at"
      ],
      "unevaluatedProperties": false
    },
    "ServiceProbeResult": {
      "title": "ServiceProbeResult",
      "description": "The answer to one runtime probe. Deliberately distinct from `SuccessResponseEnvelope` / `ErrorResponseEnvelope`: it carries no `result`/`error` branch and no negotiated authority, because a probe answers a transport-level question, not an application operation.",
      "type": "object",
      "properties": {
        "probe": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ProbeKind",
          "description": "Echoes the probe that was requested."
        },
        "status": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ProbeStatus",
          "description": "Overall outcome for this probe."
        },
        "server_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ReleaseVersion",
          "description": "Concrete server build version answering the probe."
        },
        "api_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ContractVersion",
          "description": "API contract version this server build implements."
        },
        "observed_at": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Timestamp",
          "description": "When this probe was answered."
        },
        "components": {
          "type": "array",
          "description": "Per-subsystem detail, for a health or readiness probe.",
          "items": {
            "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ServiceComponentStatus"
          },
          "maxItems": 256
        },
        "supported_capabilities": {
          "type": "array",
          "description": "Capabilities this server build implements, for a discovery probe. This is a runtime discovery fact only: an unauthenticated probe has no caller and no workspace to authorize against, so it must never state `granted` or `effective` authority. A caller must still negotiate `CapabilitySet.granted`/`effective` through an authenticated request.",
          "items": {
            "$ref": "https://contracts.omnivia.dev/application/v1/compatibility.schema.json#/$defs/CapabilityRef"
          },
          "maxItems": 256
        },
        "descriptor": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/ServiceEndpointDescriptor",
          "description": "Endpoint descriptor for this service instance, for a discovery probe. Like `supported_capabilities`, it is a runtime discovery fact: it states what this build is and where it can be reached, never what the caller may do. Optional, because a health or readiness probe answers without one."
        },
        "details": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/JsonObject",
          "description": "Optional structured detail."
        }
      },
      "required": ["probe", "status", "server_version", "api_version", "observed_at"],
      "unevaluatedProperties": false
    },
    "CoreTargetKind": {
      "title": "CoreTargetKind",
      "description": "Closed vocabulary naming how a Core target is reached: `local` for a same-host instance, `private_remote` for an instance reached over a private, self-hosted network, `cloud` for a hosted multi-tenant instance. Closed to exactly these three; an unknown kind fails closed rather than being guessed at.",
      "type": "string",
      "enum": ["local", "private_remote", "cloud"]
    },
    "CoreTargetManagement": {
      "title": "CoreTargetManagement",
      "description": "Closed vocabulary naming who owns a Core target's process lifecycle: `locally_managed` when this client started and owns the instance, `externally_managed` when some other process or operator owns it. Closed to exactly these two; an unknown value fails closed. A lifecycle action such as `start`/`stop`/`restart` is only ever safe to offer for a `locally_managed` target, since only the owner of a process may start or stop it.",
      "type": "string",
      "enum": ["locally_managed", "externally_managed"]
    },
    "CoreTargetV1": {
      "title": "CoreTargetV1",
      "description": "The provider-neutral identity of one Core target a client may select and address: what kind of instance it is, which workspace it serves, who manages its process lifecycle, and an opaque reference to its endpoint profile. Deliberately carries no dialable endpoint, credential, process identity, or filesystem path of its own; resolving `endpoint_profile_ref` to a concrete transport address is a separate, authenticated step this descriptor does not perform.",
      "type": "object",
      "properties": {
        "contract_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ContractVersion",
          "description": "Contract version this target descriptor is published at."
        },
        "target_ref": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Identifier",
          "description": "Stable, caller-opaque identifier of this target. Never a workspace, service instance, or installation identifier -- selecting a target is a separate concept from any of those."
        },
        "display_name": {
          "type": "string",
          "description": "Human-readable name for this target.",
          "minLength": 1,
          "maxLength": 256
        },
        "kind": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreTargetKind",
          "description": "How this target is reached."
        },
        "workspace_ref": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/WorkspaceId",
          "description": "Workspace this target serves."
        },
        "management": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreTargetManagement",
          "description": "Who owns this target's process lifecycle."
        },
        "endpoint_profile_ref": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/Identifier",
          "description": "Opaque reference to this target's endpoint profile. Never a dialable URI, credential, or filesystem path; resolving it to a concrete transport address is a separate, authenticated step this reference does not perform."
        }
      },
      "required": [
        "contract_version",
        "target_ref",
        "display_name",
        "kind",
        "workspace_ref",
        "management",
        "endpoint_profile_ref"
      ],
      "unevaluatedProperties": false
    },
    "CoreLifecycleState": {
      "title": "CoreLifecycleState",
      "description": "Closed, normalized vocabulary for a target's lifecycle phase, deliberately narrower than a provider's raw lifecycle codes so a safe status may publish it pre-authentication. `failed` is a real service failure -- the process reached a terminal error rather than a requested stop -- and is distinct from `stopped`, which is the ordinary not-running phase; neither carries a reason, because a safe status may not publish one. Closed to exactly these values; an unknown value fails closed.",
      "type": "string",
      "enum": ["starting", "running", "stopping", "stopped", "failed", "unknown"]
    },
    "CoreReadinessState": {
      "title": "CoreReadinessState",
      "description": "Closed, normalized vocabulary for whether a target is ready to serve requests now. Closed to exactly these values; an unknown value fails closed.",
      "type": "string",
      "enum": ["ready", "not_ready", "unknown"]
    },
    "CoreCompatibilityState": {
      "title": "CoreCompatibilityState",
      "description": "Closed, normalized vocabulary mirroring `compatibility.schema.json`'s `x-omnivia-compatibility-statuses`, restated here as a closed enum because a safe status is published pre-authentication and must fail closed on a value it does not recognize rather than forward an unrecognized open code to that surface.",
      "type": "string",
      "enum": ["compatible", "compatible_with_deprecations", "upgrade_required", "incompatible", "unknown"]
    },
    "CoreConnectionState": {
      "title": "CoreConnectionState",
      "description": "Closed, normalized vocabulary for a target's transport connection state. `authentication_required` is the normalized state of a target that is reachable but will not serve this caller until it authenticates -- distinct from `disconnected`, which says nothing about why. The matching `authentication_required` `CoreSafeWarningCode` stays the actionable advisory a caller surfaces; this field is the state. Closed to exactly these values; an unknown value fails closed.",
      "type": "string",
      "enum": [
        "connected",
        "connecting",
        "disconnected",
        "unreachable",
        "authentication_required",
        "unknown"
      ]
    },
    "CoreSafeWarningCode": {
      "title": "CoreSafeWarningCode",
      "description": "Closed vocabulary of non-fatal advisories a safe status may attach. Deliberately a closed enum rather than `common.schema.json`'s open `OpenCode`: a safe status is published pre-authentication and must never carry a free-form reason or an unrecognized code a caller cannot reason about.",
      "type": "string",
      "enum": [
        "endpoint_unreachable",
        "authentication_required",
        "version_incompatible",
        "upgrade_required",
        "workspace_format_incompatible",
        "degraded"
      ]
    },
    "CoreSafeAction": {
      "title": "CoreSafeAction",
      "description": "Closed vocabulary of actions a caller may be permitted to attempt against a target, given only what a safe status may say pre-authentication. `start`/`stop`/`restart` are process-lifecycle actions and must only be offered for a `locally_managed` `local` target (see `CoreSafeStatusV1.permitted_actions`); `reconnect` and `open` are safe to offer for any target.",
      "type": "string",
      "enum": ["start", "stop", "restart", "reconnect", "open"]
    },
    "CoreSafeStatusV1": {
      "title": "CoreSafeStatusV1",
      "description": "A Core target's status, narrowed to values safe to publish before authentication or to any caller regardless of authority. It carries no dialable endpoint, process identity, filesystem path, credential, token, callback, account, entitlement, vault reference, stack trace, raw exception, or free-form reason: every advisory is one of the closed `CoreSafeWarningCode` values, and every offered action is one of the closed `CoreSafeAction` values.",
      "type": "object",
      "properties": {
        "contract_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ContractVersion",
          "description": "Contract version this status is published at."
        },
        "target": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreTargetV1",
          "description": "The target this status describes."
        },
        "lifecycle_state": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreLifecycleState",
          "description": "Normalized lifecycle phase."
        },
        "readiness_state": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreReadinessState",
          "description": "Normalized readiness to serve requests."
        },
        "compatibility_state": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreCompatibilityState",
          "description": "Normalized version compatibility posture."
        },
        "connection_state": {
          "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreConnectionState",
          "description": "Normalized transport connection state."
        },
        "server_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ReleaseVersion",
          "description": "Concrete server build version, when safe to disclose and known."
        },
        "protocol_version": {
          "$ref": "https://contracts.omnivia.dev/application/v1/common.schema.json#/$defs/ContractVersion",
          "description": "Wire protocol version this target is speaking, when known."
        },
        "warning_codes": {
          "type": "array",
          "description": "Non-fatal advisories, each a closed `CoreSafeWarningCode`. No two entries may repeat the same code.",
          "items": {
            "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreSafeWarningCode"
          },
          "uniqueItems": true,
          "maxItems": 32
        },
        "permitted_actions": {
          "type": "array",
          "description": "Actions the caller may attempt against this target, each a closed `CoreSafeAction`. `start`/`stop`/`restart` are present only when `target.management` is `locally_managed` and `target.kind` is `local`. No two entries may repeat the same action.",
          "items": {
            "$ref": "https://contracts.omnivia.dev/application/v1/service.schema.json#/$defs/CoreSafeAction"
          },
          "uniqueItems": true,
          "maxItems": 16
        }
      },
      "required": [
        "contract_version",
        "target",
        "lifecycle_state",
        "readiness_state",
        "compatibility_state",
        "connection_state",
        "warning_codes",
        "permitted_actions"
      ],
      "unevaluatedProperties": false
    }
  }
}
