{
  "name": "omnivia-core",
  "version": "0.1.0",
  "private": true,
  "description": "Portable knowledge contracts, graph fragments, validators, normalizers, and public examples for OmniVia Core.",
  "scripts": {
    "test": "echo \"Core tests are provided by the Python packages; run package-specific checks.\"",
    "lint": "echo \"Core linting is package-specific.\""
  }
}
